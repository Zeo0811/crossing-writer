{
  "name": "@crossing/agents",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p . && pnpm run copy-prompts",
    "copy-prompts": "mkdir -p dist/prompts && cp src/prompts/*.md dist/prompts/",
    "test": "vitest run",
    "dev": "tsc -w -p ."
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
